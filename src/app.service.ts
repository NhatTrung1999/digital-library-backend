import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {
  // constructor(@Inject('LYG_DL') private readonly db: Sequelize) {}
  // getHello() {
  //   return this.db.query(`SELECT * FROM DL_Users`);
  // }
}
